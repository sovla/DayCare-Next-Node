/* eslint-disable no-unused-vars */
/* eslint-disable no-param-reassign */
/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  // images: {
  //   loader: 'imgix',
  //   path: 'https://daycare-center.shop/',
  // },
};

module.exports = nextConfig;
